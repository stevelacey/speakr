homepage:
  url:   /
  param:
    module: homepage
    action: index

login:
  url:   /login
  param:
    module: sfTwitterAuth
    action: login

# generic rules
# please, remove them by adding more specific rules
default_index:
  url:   /:module
  param: { action: index }

default:
  url:   /:module/:action/*